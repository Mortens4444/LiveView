SELECT 
    ID as Id,
    date as Date,
    user_id as UserId,
    operation_id as OperationId,
    event_id as EventId,
    language_element_id as LanguageElementId,
    other_information as OtherInformation,
    checksum as Checksum
FROM Logs;
